void main(List<String> args) {
  const myList = [1, 2, 3];

  // ! can't assign a value to const variable
  // myList = [4, 5, 6];

  // ! also can't mutate contents of const variable
  // myList.add(5);

  print(myList);
}
